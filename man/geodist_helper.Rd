% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_indices.R
\name{geodist_helper}
\alias{geodist_helper}
\title{Calculate geographic distance via Haversine formula (helper)}
\usage{
geodist_helper(vec_from, vec_to = NULL, coord_cols = c("x", "y"))
}
\arguments{
\item{vec_from}{Numeric vector of length 2, or a named vector containing
coordinates. If named, should include names given by \code{coord_cols}.}

\item{vec_to}{Optional numeric vector of length 2 (destination coordinates);
if \code{NULL} (default), the function returns 0.}

\item{coord_cols}{Character vector of length 2 giving the names of the longitude
and latitude elements in \code{vec_from}/\code{vec_to} when those are named vectors.
Defaults to \code{c("x", "y")}.}
}
\value{
Numeric distance in meters between the two points, or 0 if \code{vec_to} is \code{NULL}.
}
\description{
Computes the geographic (Haversine) distance in meters between two coordinate
pairs, or returns 0 for single-site (order=1) calculations when \code{vec_to} is \code{NULL}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_indices.R
\name{orderwise_diss_gower}
\alias{orderwise_diss_gower}
\title{Compute Gower dissimilarity between two site vectors}
\usage{
orderwise_diss_gower(vec_from, vec_to = NULL)
}
\arguments{
\item{vec_from}{Numeric vector representing the first site's attributes (e.g.,
species counts or trait values).}

\item{vec_to}{Optional numeric vector for the second site; if \code{NULL}, the
function returns \code{NA_real_}.}
}
\value{
A single numeric dissimilarity value between 0 and 1, or \code{NA_real_} if
\code{vec_to} is \code{NULL}.
}
\description{
Calculates the Gower dissimilarity (a bounded 0–1 measure) between two numeric
vectors (e.g., species abundances) using the \code{cluster::daisy()} function. When
the second vector is \code{NULL} (order 1), returns \code{NA_real_} since a pairwise
comparison is not meaningful.
}
